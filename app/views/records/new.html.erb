<%= @collection.name %>

Add A Book

<%= form_for @record do |f| %>
    <div id="form">
      <%= f.label :title, "Title" %>
      <%= f.text_field :title %>

      <%= f.submit "Add Book" %>
    </div>
<% end %>

